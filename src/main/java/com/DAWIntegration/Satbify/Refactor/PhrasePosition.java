package com.DAWIntegration.Satbify.Refactor;

// position of a Phrase in a Period. Usually period contains only two phrases (sentences)
public enum PhrasePosition {
    BEGINNING,
    MIDDLE,     // rare
    END
}
